apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

images:
  - name: ntfy
    newName: binwiederhier/ntfy
    newTag: v2.21.0

namespace: ntfy

resources:
  - ntfy-deployment.yaml
  - ntfy-svc.yaml
  - ntfy-pvc.yaml
  - ntfy-ingress.yaml

configMapGenerator:
  - name: server-config
    files: 
      - server.yml
