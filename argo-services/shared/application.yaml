apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shared
  namespace: argo
spec:
  destination:
    namespace: shared
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: git@github.com:MohanadAbugharbia/homelab.git
    path: argo-services/shared
    targetRevision: HEAD
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
